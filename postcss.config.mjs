export default {
  plugins: { tailwindcss: {} },
};


/*

const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
*/

/*
default one

const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
*/
